<% if @post.errors.any? %>
    <h2><%= pluralize( @post.errors.count, "error") %> occured</h2>
    <ul>
        <% @post.errors.full_messages.each do |error| %>
        <li><%= error %></li>
        <% end %>
    </ul> 
<% end %>
